name: Playground

on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/playground.yml']
    tags:
      - '*'

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
    - run: echo '${{ toJSON(github) }}'
