name: Playground

on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
      - if: fromJSON('{}').shuffle != false
        run: echo 123
      - if: fromJSON('{"shuffle": false}').shuffle != false
        run: echo 456
