name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

jobs:
  playground:
    runs-on: windows-latest
    name: ${{ matrix.target }}
    steps:
      - run: env
      
