name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: macos-latest
    steps:
      - run: echo "PGDATA=/usr/local/var/postgres" >> $GITHUB_ENV
      - run: pg_ctl start
      - run: ps auxw |  grep postgres | grep -- -D
