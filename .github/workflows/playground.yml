name: Playground

on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    env:
      JSON: '{"shuffle": false}'
      JSON2: '{"shuffle": "false"}'
      JSON3: '{"shuffle": true}'
      JSON4: '{"shuffle": "true"}'
      JSON5: '{"shuffle": "abc"}'
    steps:
      - if: fromJSON('{}').shuffle == '' || fromJSON('{}').shuffle == true
        run: echo 123
      - if: fromJSON(env.JSON).shuffle == '' || fromJSON(env.JSON).shuffle == true
        run: echo 123
      - if: fromJSON(env.JSON2).shuffle == '' || fromJSON(env.JSON2).shuffle == true
        run: echo 123
      - if: fromJSON(env.JSON3).shuffle == '' || fromJSON(env.JSON3).shuffle == true
        run: echo 123
      - if: fromJSON(env.JSON4).shuffle == '' || fromJSON(env.JSON4).shuffle == true
        run: echo 123
      - if: fromJSON(env.JSON5).shuffle == '' || fromJSON(env.JSON5).shuffle == true
        run: echo 123
