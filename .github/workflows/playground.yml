name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

jobs:
  playground:
    runs-on: ${{ github.repository == 'galargh/.github' && fromJSON('["ubuntu-22.04"]') || 'ubuntu-20.04' }}
    steps:
      - run: lsb_release -a
