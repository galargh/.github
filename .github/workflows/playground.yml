name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml', '**/*.yml']
  release:
    types:
      - published
      - released

permissions:
  checks: write

defaults:
  run:
    shell: bash

jobs:
  playground:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello'
  playground2:
    needs: ${{ fromJSON(false && '["playground"]' || '[]') }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Goodbye'
