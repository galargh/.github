name: Playground

on:
  workflow_dispatch:
  workflow_call:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml', '**/*.yml']
  release:
    types:
      - published
      - released

permissions:
  checks: write

defaults:
  run:
    shell: bash

env:
  ABC: 123

jobs:
  playground:
    uses: ./.github/workflows/playground4.yml
    strategy:
      matrix:
        include:
          - name: Playground 1
            description: Playground 1
            required: true
          - name: Playground 2
            description: Playground 2
            required: true
          - name: Playground 3
            description: Playground 3
            required: true
    name: playground (${{ matrix.required }}) [required]
    with:
      name: ${{ matrix.name }}


