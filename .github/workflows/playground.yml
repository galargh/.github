name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    env:
      DEFAULTS_Go: |
        {
          "lang": "Go"
        }
    steps:
      - env:
          DEFAULTS: ${{ env["DEFAULTS_Go"] || '{}' }}
          DEFAULTS2: ${{ env["DEFAULTS_Js"] || '{}' }}
        run: echo "$DEFAULTS" "$DEFAULTS2"

