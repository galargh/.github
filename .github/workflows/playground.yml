name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
      - name: Create PRs
        uses: actions/github-script@v6
        with:
          retries: 0
          script: |
            const request = async function(req, opts) {
              console.log(opts)
              opts.request.retries = 0
              opts.retries = 0
              return await req(opts)
            }
            github.hook.wrap('request', request)

            try {
              await github.rest.repos.listForAuthenticatedUser({affiliation: 'collaborator'})
            } catch(e) {}
            try {
              await github.rest.repos.listForAuthenticatedUser({affiliation: 'collaborator'})
            } catch(e) {}
