name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']
  release:
    types:
      - published
      - released

jobs:
  playground:
    runs-on: ['self-hosted', 'linux', 'x64', 'xlarge']
    steps:
      - run: |
          echo "${{ join(fromJSON('hello') || fromJSON('["hello", "goodbye"]'), '\n') }}"
        shell: bash
