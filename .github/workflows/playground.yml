name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml', '**/*.yml']
  release:
    types:
      - published
      - released

permissions:
  checks: write

defaults:
  run:
    shell: bash

jobs:
  playground:
    if: false
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package:
         - a
         - b
    steps:
      - run: echo 'Hello'
  playground2:
    needs: playground
    if: always()
    runs-on: ubuntu-latest
    steps:
      - run: echo '${{ needs.playground.status }}'
