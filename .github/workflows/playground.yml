name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    name: ${{ matrix.target }}
    steps:
      - env:
          A: ${{ fromJSON('{}').gogenerate == true }}
          B: ${{ fromJSON('{}').gogenerate == 'true' }}
          C: ${{ fromJSON('{}').gogenerate != 'false' }}
        run: echo "$A $B $C"
