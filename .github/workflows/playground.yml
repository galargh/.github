name: Playground

on:
  workflow_dispatch:
    inputs:
      test:
        description: a
        required: false
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
      - run: echo '${{ toJSON(github.event.inputs) }}'
      
