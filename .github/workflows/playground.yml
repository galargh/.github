name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']
  release:
    types:
      - published
      - released

permissions:
  checks: write

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
      - run: gh api -X PATCH /repos/galargh/.github/check-runs/23453625057 -f completed_at='2000-01-01T00:00:00Z'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        shell: bash
          
