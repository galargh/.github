name: Playground

on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    uses: galargh/.github/.github/workflows/playground2.yml@master
    with:
      abc: ${{ vars.ABC }}
