name: Playground
on:
  pull_request:
#  workflow_dispatch:
#  push:
#    paths: [.github/workflows/playground.yml]

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
      - env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh --repo "${{ github.repository }}" pr comment ${{ github.event.pull_request.number }} --body "WOW"
