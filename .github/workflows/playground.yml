name: Playground

on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    env:
      JSON: '{"shuffle": false}'
      JSON2: '{"shuffle": "false"}'
      JSON3: '{"shuffle": true}'
      JSON4: '{"shuffle": "true"}'
      JSON5: '{"shuffle": "abc"}'
    steps:
      - if: fromJSON('{}').shuffle == '' || fromJSON('{}').shuffle
        run: echo true
      - if: fromJSON(env.JSON).shuffle == '' || fromJSON(env.JSON).shuffle
        run: echo '${{fromJSON(env.JSON).shuffle}}'
      - if: fromJSON(env.JSON).shuffle == '' || fromJSON(env.JSON2).shuffle
        run: echo true
      - if: fromJSON(env.JSON).shuffle == '' || fromJSON(env.JSON3).shuffle
        run: echo true
      - if: fromJSON(env.JSON).shuffle == '' || fromJSON(env.JSON4).shuffle
        run: echo true
      - if: fromJSON(env.JSON).shuffle == '' || fromJSON(env.JSON5).shuffle
        run: echo true
