name: Playground

on:
  workflow_dispatch:
  pull_request:
  push:
    paths: ['.github/workflows/playground.yml']

defaults:
  run:
    shell: bash

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo -e "$this\n$(git tag)" | sort -V | tail -n1 | xargs -I{} echo "latest={}"
      - run: echo -e "$this\n$(git tag)" | sort -V | tail -n1
      - run: echo -e "$this\n$(git tag)" | sort -V
      - run: echo -e "$this\n$(git tag)"
      - run: git tag


