name: Playground
on:
  workflow_dispatch:
    inputs:
      test:
        required: false
        description: Hello
  push:
    paths: [.github/workflows/playground.yml]

jobs:
  playground:
    runs-on: ubuntu-latest
    steps:
      - if: true && ! github.event.inputs.test
        run: echo "Hello"
