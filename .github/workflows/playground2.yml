on:
  workflow_run:
    workflows: 
      - Playground
    types:
      - completed

jobs:
  playground2:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include: ${{ github.event.workflow_run.pull_requests }}
    steps:
      - run: echo "${{ matrix.number }} ${{ matrix.head }}"
