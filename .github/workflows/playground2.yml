on:
  workflow_call:
    inputs:
      secret:
        default: Hello
        type: string
        required: false

jobs:
  playground2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ toJSON(githu) }}"
        shell: bash
      - run: env
        shell: bash
      - run: gh run view --repo ${{ github.repository }} ${{ github.run_id }}
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
