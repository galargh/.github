on:
  workflow_call:
    inputs:
      secret:
        default: ${{ secrets.GALARGH_GITHUB_TOKEN }}
        type: string
        required: false

jobs:
  playground2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ toJSON(github.event.inputs) }}"
        shell: bash
